{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/spiffcs/hoard/main/schema/json/schema-1.1.1.json",
  "$defs": {
    "Binder": {
      "properties": {
        "id": {
          "type": "integer"
        },
        "name": {
          "type": "string"
        },
        "isDefault": {
          "type": "boolean",
          "description": "IsDefault marks the built-in binder every database starts with — the one\n`hoard binder rm` refuses. It is on the row rather than implied by\nposition so a consumer need not rely on the ordering to know which."
        },
        "distinctCards": {
          "type": "integer"
        },
        "totalCopies": {
          "type": "integer"
        },
        "valueUsd": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "id",
        "name",
        "isDefault",
        "distinctCards",
        "totalCopies",
        "valueUsd"
      ],
      "description": "Binder is one binder, carrying the id the rest of the CLI takes as an argument."
    },
    "Binders": {
      "properties": {
        "rows": {
          "items": {
            "$ref": "#/$defs/Binder"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "rows"
      ],
      "description": "Binders is every binder with its rolled-up counts and value, the default binder first and the rest by name — the order `hoard binder list` prints."
    },
    "Card": {
      "properties": {
        "name": {
          "type": "string"
        },
        "scryfallId": {
          "type": "string"
        },
        "mtgjsonUuid": {
          "type": "string"
        },
        "setCode": {
          "type": "string"
        },
        "number": {
          "type": "string"
        },
        "finish": {
          "type": "string",
          "enum": [
            "nonfoil",
            "foil",
            "etched"
          ]
        },
        "condition": {
          "type": "string",
          "description": "Condition is the copy's wear — \"nm\", \"lp\", \"mp\", \"hp\", \"dmg\" — absent\nwhen nobody has assessed it, which is most holdings. It describes the\ncopies held rather than the printing, so two entries of one printing can\ndiffer by it.\n\nIt never affects a value in these documents: no source hoard reads\npublishes a per-condition price, so every copy is valued at the\nprinting's price whatever its condition."
        },
        "lang": {
          "type": "string",
          "description": "Lang is Scryfall's language code for the printing (\"en\", \"ja\", \"zhs\"),\nabsent when hoard has not stored the card's document. Language is part\nof a printing's identity — Scryfall mints a distinct id per language —\nso this names which of them ScryfallID refers to rather than adding a\ndimension to it."
        },
        "colorIdentity": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "ColorIdentity is Scryfall's color_identity: WUBRG letters, empty for a\ncolorless card, absent when the identity is not known to hoard (the\ncard's document was never fetched, or the emitting query predates it)."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "scryfallId",
        "setCode",
        "number",
        "finish"
      ],
      "description": "Card identifies one printing in one finish."
    },
    "CardDetail": {
      "properties": {
        "rarity": {
          "type": "string",
          "description": "Rarity is Scryfall's: common, uncommon, rare, mythic, special or bonus."
        },
        "typeLine": {
          "type": "string",
          "description": "TypeLine is the printed type line (\"Legendary Creature — Elf Druid\"), the front face's on a two-faced card."
        },
        "cmc": {
          "type": "number",
          "description": "Cmc is the mana value. Zero is a real value — every land has it — so this is absent only when unknown."
        },
        "manaCost": {
          "type": "string",
          "description": "ManaCost is the printed cost in Scryfall's brace notation (\"{2}{W}{U}\"), absent for a card with no cost."
        },
        "oracleText": {
          "type": "string",
          "description": "OracleText is the current rules text, newline-separated, with reminder text as Scryfall stores it."
        },
        "power": {
          "type": "string",
          "description": "Power is the creature's, as printed — text because the game prints \"*\" and \"1+*\" there."
        },
        "toughness": {
          "type": "string",
          "description": "Toughness is the creature's, as printed, and text for the same reason as power."
        },
        "loyalty": {
          "type": "string",
          "description": "Loyalty is the planeswalker's starting loyalty, as printed."
        },
        "setName": {
          "type": "string",
          "description": "SetName is the set's full name (\"Commander 2021\"), so a by-set breakdown reads without a code table."
        },
        "releasedAt": {
          "type": "string",
          "description": "ReleasedAt is the set's release date (YYYY-MM-DD), which sorts chronologically where a set code does not."
        },
        "artist": {
          "type": "string",
          "description": "Artist is the illustration's credited artist, as Scryfall spells it."
        },
        "layout": {
          "type": "string",
          "description": "Layout is Scryfall's layout (\"normal\", \"split\", \"transform\", \"modal_dfc\"), which says how many faces to expect."
        },
        "flavorText": {
          "type": "string",
          "description": "FlavorText is the printed flavor text, absent on a printing with none."
        },
        "promoTypes": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "PromoTypes are Scryfall's variant tags (\"surgefoil\", \"showcase\"), absent on an ordinary printing."
        },
        "printedName": {
          "type": "string",
          "description": "PrintedName is the name in the card's own language and script, absent on an English printing."
        },
        "tcgplayerId": {
          "type": "integer",
          "description": "TcgplayerID is TCGplayer's product id for the printing — a marketplace join key, and printing-level, so it does not distinguish finishes."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "CardDetail is one printing's card characteristics, every one of them derived from the Scryfall document hoard has stored for it."
    },
    "Comp": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "copies": {
          "type": "integer"
        },
        "valueUsd": {
          "type": "number"
        },
        "marketUsd": {
          "type": "number",
          "description": "MarketUsd is tcgplayer's sales-derived market price."
        },
        "cardKingdomUsd": {
          "type": "number"
        },
        "manapoolUsd": {
          "type": "number"
        },
        "lowUsd": {
          "type": "number"
        },
        "lowFrom": {
          "type": "string"
        },
        "buylistUsd": {
          "type": "number"
        },
        "buylistTo": {
          "type": "string"
        },
        "spread": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "copies",
        "valueUsd",
        "lowUsd",
        "lowFrom"
      ],
      "description": "Comp is one owned printing's comp sheet: each vendor's USD figure for the owned finish."
    },
    "Container": {
      "properties": {
        "name": {
          "type": "string"
        },
        "kind": {
          "type": "string",
          "enum": [
            "binder",
            "deck"
          ]
        },
        "source": {
          "type": "string",
          "description": "Source is the provider a container came from — \"manual\" for a binder,\nthe site's slug for a deck."
        },
        "sourceId": {
          "type": "string"
        },
        "sourceUrl": {
          "type": "string"
        },
        "format": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "kind"
      ],
      "description": "Container is one binder or deck, carrying the identity a flat holdings row cannot."
    },
    "DeckAggregate": {
      "properties": {
        "count": {
          "type": "integer"
        },
        "totalCopies": {
          "type": "integer"
        },
        "valueUsd": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "count",
        "totalCopies",
        "valueUsd"
      ],
      "description": "DeckAggregate rolls every deck into one line, as the report's totals show them; the per-deck split lives in the summary document."
    },
    "DeckTotals": {
      "properties": {
        "name": {
          "type": "string"
        },
        "distinctCards": {
          "type": "integer"
        },
        "totalCopies": {
          "type": "integer"
        },
        "valueUsd": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "distinctCards",
        "totalCopies",
        "valueUsd"
      ],
      "description": "DeckTotals is one deck's totals, ordered by value as the summary lists them."
    },
    "FiredWatch": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "op": {
          "type": "string",
          "enum": [
            "under",
            "over",
            "drop",
            "rise"
          ]
        },
        "thresholdUsd": {
          "type": "number"
        },
        "priceUsd": {
          "type": "number"
        },
        "percent": {
          "type": "number",
          "description": "Percent is the movement the watch asks about, as a fraction."
        },
        "anchorUsd": {
          "type": "number"
        },
        "anchorAt": {
          "type": "string"
        },
        "movedPct": {
          "type": "number",
          "description": "MovedPct is the movement actually observed, signed: -0.113 is down\n11.3%. It is derivable from PriceUsd and AnchorUsd and is here anyway,\nbecause it is the entire content of the alert and a consumer should not\nhave to recompute the thing being reported to it."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "op",
        "priceUsd"
      ],
      "description": "FiredWatch is one alert."
    },
    "GrandTotal": {
      "properties": {
        "totalCopies": {
          "type": "integer"
        },
        "valueUsd": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "totalCopies",
        "valueUsd"
      ],
      "description": "GrandTotal is the whole hoard."
    },
    "Guessed": {
      "properties": {
        "rows": {
          "items": {
            "$ref": "#/$defs/GuessedRow"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "rows"
      ],
      "description": "Guessed is the audit queue the hands-free scanner's nonfoil default creates: scanned rows committed on a default rather than on evidence, newest first."
    },
    "GuessedRow": {
      "properties": {
        "id": {
          "type": "integer"
        },
        "card": {
          "$ref": "#/$defs/Card"
        },
        "guessedAt": {
          "type": "string",
          "description": "GuessedAt is when the scanner committed the row, as the guess log stores\nit — the column the text listing prints beside each row."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "id",
        "card",
        "guessedAt"
      ],
      "description": "GuessedRow is one unchecked guess."
    },
    "Hoard": {
      "properties": {
        "databaseVersion": {
          "type": "integer",
          "description": "DatabaseVersion is the source database's SQLite user_version — hoard's\nstorage schema, which versions independently of this document's\nschemaVersion."
        },
        "printings": {
          "items": {
            "$ref": "#/$defs/Printing"
          },
          "type": "array"
        },
        "containers": {
          "items": {
            "$ref": "#/$defs/Container"
          },
          "type": "array"
        },
        "holdings": {
          "$ref": "#/$defs/Holdings"
        },
        "watches": {
          "items": {
            "$ref": "#/$defs/Watch"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "databaseVersion",
        "printings",
        "containers",
        "holdings",
        "watches"
      ],
      "description": "Hoard is a whole hoard as one interchange document — the payload `hoard merge` moves between two databases."
    },
    "Holding": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "detail": {
          "$ref": "#/$defs/CardDetail",
          "description": "Detail is what the printing's Scryfall document says the card is —\nrarity, type line, mana value, oracle text and the rest — enough to\ncompute a curve, a rarity breakdown or a text search without fetching\nanything. It is absent when hoard has stored no document for the\nprinting; a field inside it is absent when the card has no such value,\nso an artifact carries no power and an English printing no printedName.\n\nA hoard document never carries it: every printing there embeds its\nScryfall document verbatim in printings[].raw, which is strictly more\nthan this, and adding derived copies would move the content hash `hoard\nmerge` uses to refuse a source it has already applied."
        },
        "count": {
          "type": "integer"
        },
        "container": {
          "type": "string"
        },
        "containerKind": {
          "type": "string",
          "enum": [
            "binder",
            "deck"
          ]
        },
        "board": {
          "type": "string"
        },
        "priceUsd": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "count",
        "container",
        "containerKind",
        "board"
      ],
      "description": "Holding is a quantity of one card in one container."
    },
    "Holdings": {
      "properties": {
        "rows": {
          "items": {
            "$ref": "#/$defs/Holding"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "rows"
      ],
      "description": "Holdings is every requested holding, one row per printing-finish-container, in the export's canonical order — the same data as the canonical CSV."
    },
    "Market": {
      "properties": {
        "comparedPrintings": {
          "type": "integer",
          "description": "ComparedPrintings is how many owned printings had two or more vendors,\nso a consumer knows how much ground the analysis covered."
        },
        "opportunities": {
          "items": {
            "$ref": "#/$defs/Opportunity"
          },
          "type": "array"
        },
        "comps": {
          "items": {
            "$ref": "#/$defs/Comp"
          },
          "type": "array",
          "description": "Comps is every compared printing's per-vendor sheet, ordered by\nvalueUsd descending — the data behind the COMPS table."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "comparedPrintings",
        "opportunities",
        "comps"
      ],
      "description": "Arbitrage is one pass of vendor disagreement over everything held: the full ranking behind all three tables, not the display's top-N."
    },
    "Movers": {
      "properties": {
        "since": {
          "type": "string",
          "description": "Since is the comparison cutoff (RFC 3339): each card's newest price\nagainst its last price recorded on or before this instant."
        },
        "recordedSince": {
          "type": "string",
          "description": "RecordedSince is the oldest price observation held (RFC 3339). When it\nis later than Since, history simply does not reach as far back as the\nquestion asked."
        },
        "changes": {
          "items": {
            "$ref": "#/$defs/PriceChange"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "since",
        "changes"
      ],
      "description": "Movers is the price changes between two observations, every change rather than the display's top-N, ordered by absolute impact."
    },
    "Opportunity": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "copies": {
          "type": "integer"
        },
        "valueUsd": {
          "type": "number"
        },
        "kind": {
          "type": "string",
          "enum": [
            "arbitrage",
            "liquid",
            "below-market"
          ]
        },
        "buyUsd": {
          "type": "number"
        },
        "buyFrom": {
          "type": "string"
        },
        "marketUsd": {
          "type": "number",
          "description": "MarketUsd is tcgplayer's market price — computed from actual sales —\nwhen the printing has one; every ranking anchors on it."
        },
        "belowMarket": {
          "type": "number",
          "description": "BelowMarket is the fraction the cheapest ask sits under marketUsd."
        },
        "sellUsd": {
          "type": "number"
        },
        "sellTo": {
          "type": "string"
        },
        "profitUsd": {
          "type": "number",
          "description": "ProfitUsd is sellUsd − marketUsd per copy: what a buylist pays over\nthe last-sold price. Positive is genuine arbitrage."
        },
        "liquidity": {
          "type": "number",
          "description": "Liquidity is the fraction of marketUsd a shop will pay."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "copies",
        "valueUsd",
        "kind",
        "buyUsd",
        "buyFrom"
      ],
      "description": "Opportunity is one owned printing-and-finish with the vendor quotes that make it interesting, tagged with the question it answers: \"arbitrage\" (a shop pays more than the cheapest retail), \"liquid\" (buylist close to retail), or \"spread\" (vendors disagree)."
    },
    "PriceChange": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "copies": {
          "type": "integer"
        },
        "oldUsd": {
          "type": "number"
        },
        "newUsd": {
          "type": "number"
        },
        "oldAsOf": {
          "type": "string",
          "description": "OldAsOf is when oldUsd was observed (RFC 3339), which is not always the\ndocument's since: a printing whose record begins inside the window is\nmeasured from its own earliest price, so this row spans from here rather\nthan from the window's start. Read a change against this, not against\nsince, or rows with younger records are misread as covering the whole\nwindow."
        },
        "impactUsd": {
          "type": "number"
        },
        "pctChange": {
          "type": "number",
          "description": "PctChange is the move as a fraction of oldUsd, signed: 1.1458 is up\n114.58%. It is derivable from oldUsd and newUsd and is here for the same\nreason impactUsd beside it is — the text view prints it, so a consumer\nreading the document was reproducing a figure hoard had already computed.\n\nIt is absent, not zero, when oldUsd is 0: a rise from nothing is an\ninfinite percentage rather than a large one, and the whole point of\ncarrying the field is that hoard decides that once instead of every\ncaller deciding it differently. Absent is the model's word for \"no such\nvalue\" throughout — priceUsd, spread and belowMarket all use it — and a\nzero here would be indistinguishable from a printing that did not move."
        },
        "source": {
          "type": "string",
          "description": "Source names where the new price came from: \"scryfall\", or the vendor\nbehind a fallback."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "copies",
        "oldUsd",
        "newUsd",
        "oldAsOf",
        "impactUsd",
        "source"
      ],
      "description": "PriceChange is one printing-and-finish whose per-copy price moved."
    },
    "Printing": {
      "properties": {
        "scryfallId": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "setCode": {
          "type": "string"
        },
        "number": {
          "type": "string"
        },
        "scryfallUrl": {
          "type": "string"
        },
        "updatedAt": {
          "type": "string",
          "description": "UpdatedAt is when this row was last refreshed (RFC 3339). A merge keeps\nwhichever side's row is newer, so a stale database cannot overwrite\nfresher prices in the one receiving it."
        },
        "mtgjsonUuid": {
          "type": "string"
        },
        "priceUsd": {
          "type": "number",
          "description": "Prices are per copy in the named finish, absent when no source prices\nthat finish — absent is not free."
        },
        "priceUsdFoil": {
          "type": "number"
        },
        "priceUsdEtched": {
          "type": "number"
        },
        "raw": {
          "type": "object"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "scryfallId",
        "name",
        "setCode",
        "number",
        "scryfallUrl",
        "updatedAt"
      ],
      "description": "Printing is one row of the card catalog in full, so a hoard receiving this document can accept a card it has never seen without asking Scryfall."
    },
    "Report": {
      "properties": {
        "asOf": {
          "type": "string",
          "description": "AsOf is when the prices were observed (RFC 3339); absent when nothing\nhas ever been priced."
        },
        "total": {
          "$ref": "#/$defs/GrandTotal"
        },
        "binder": {
          "$ref": "#/$defs/Totals"
        },
        "decks": {
          "$ref": "#/$defs/DeckAggregate"
        },
        "binders": {
          "items": {
            "$ref": "#/$defs/DeckTotals"
          },
          "type": "array"
        },
        "topHoldings": {
          "items": {
            "$ref": "#/$defs/ReportHolding"
          },
          "type": "array"
        },
        "sources": {
          "items": {
            "$ref": "#/$defs/SourceCount"
          },
          "type": "array"
        },
        "unpriced": {
          "$ref": "#/$defs/UnpricedCount"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "total",
        "binder",
        "decks",
        "binders",
        "topHoldings",
        "sources",
        "unpriced"
      ],
      "description": "Report is the dated valuation: the totals, each binder, the most valuable holdings, and where every price came from."
    },
    "ReportHolding": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "copies": {
          "type": "integer"
        },
        "priceUsd": {
          "type": "number"
        },
        "valueUsd": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "copies",
        "valueUsd"
      ],
      "description": "ReportHolding is one itemized holding: the per-copy price and what the copies are worth together."
    },
    "SourceCount": {
      "properties": {
        "source": {
          "type": "string"
        },
        "printings": {
          "type": "integer"
        },
        "copies": {
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "source",
        "printings",
        "copies"
      ],
      "description": "SourceCount is one price source's coverage: distinct printing-finish combinations and the physical copies they account for."
    },
    "Summary": {
      "properties": {
        "binder": {
          "$ref": "#/$defs/Totals"
        },
        "decks": {
          "items": {
            "$ref": "#/$defs/DeckTotals"
          },
          "type": "array"
        },
        "total": {
          "$ref": "#/$defs/GrandTotal"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "binder",
        "decks",
        "total"
      ],
      "description": "Summary is the hoard's totals: the loose collection, each deck, and the grand total, as the summary table reports them."
    },
    "Totals": {
      "properties": {
        "distinctCards": {
          "type": "integer"
        },
        "totalCopies": {
          "type": "integer"
        },
        "valueUsd": {
          "type": "number"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "distinctCards",
        "totalCopies",
        "valueUsd"
      ],
      "description": "Totals rolls one container group up: how many distinct printings, how many physical copies, and their market value."
    },
    "Unpriced": {
      "properties": {
        "rows": {
          "items": {
            "$ref": "#/$defs/UnpricedRow"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "rows"
      ],
      "description": "Unpriced is every holding no source can price — the copies counting as $0.00 in every total."
    },
    "UnpricedCount": {
      "properties": {
        "printings": {
          "type": "integer"
        },
        "copies": {
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "printings",
        "copies"
      ],
      "description": "UnpricedCount is the residue no source covers, counted the same way as SourceCount."
    },
    "UnpricedRow": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "copies": {
          "type": "integer"
        },
        "containers": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "copies",
        "containers"
      ],
      "description": "UnpricedRow is one unpriced card-and-finish and the containers holding it."
    },
    "Watch": {
      "properties": {
        "card": {
          "$ref": "#/$defs/Card"
        },
        "op": {
          "type": "string",
          "enum": [
            "under",
            "over",
            "drop",
            "rise"
          ]
        },
        "threshold": {
          "type": "number"
        },
        "percent": {
          "type": "number",
          "description": "Percent is the movement that fires the alert, as a fraction: 0.1 is a\nten percent move. A fraction rather than 10, because the document is\nread by scripts and a percent sign's worth of ambiguity in a number that\nmultiplies prices is not worth the readability."
        },
        "minMoveUsd": {
          "type": "number"
        },
        "sinceDays": {
          "type": "integer"
        },
        "display": {
          "type": "string",
          "description": "Display is the name the alert prints, kept as the source hoard wrote it."
        },
        "createdAt": {
          "type": "string",
          "description": "CreatedAt is when the watch was first set (RFC 3339)."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "card",
        "op",
        "display"
      ],
      "description": "Watch is one standing alert on one printing and finish, in one direction."
    },
    "WatchCheck": {
      "properties": {
        "checked": {
          "type": "integer"
        },
        "fired": {
          "items": {
            "$ref": "#/$defs/FiredWatch"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "checked",
        "fired"
      ],
      "description": "WatchCheck is one pass over the price watches: how many could be evaluated (an unpriced card answers neither \"under\" nor \"over\" and is not counted), and the watches that just crossed into their condition."
    },
    "WatchRow": {
      "properties": {
        "id": {
          "type": "integer",
          "description": "ID is the handle `hoard watch rm` takes. The text table deliberately has\nno ID column — an ambiguous name fragment prints the ids at the moment\nthey are needed, which is better targeted than a column on every row —\nbut that reasoning is about a terminal's width budget and does not reach\na document, where the id is the only unambiguous way to name a row."
        },
        "card": {
          "$ref": "#/$defs/Card"
        },
        "op": {
          "type": "string",
          "enum": [
            "under",
            "over",
            "drop",
            "rise"
          ]
        },
        "display": {
          "type": "string",
          "description": "Display is the name the alert prints, as resolved when the watch was set."
        },
        "thresholdUsd": {
          "type": "number"
        },
        "percent": {
          "type": "number",
          "description": "Percent is the movement that fires the alert, as a fraction: 0.1 is ten\npercent, matching Watch.Percent rather than the flag's \"10%\"."
        },
        "minMoveUsd": {
          "type": "number"
        },
        "sinceDays": {
          "type": "integer"
        },
        "priceUsd": {
          "type": "number",
          "description": "PriceUsd is hoard's effective price for the watched finish, absent when\nno source prices it — absent is not free, and a watch with no price\nanswers none of the four questions."
        },
        "anchorUsd": {
          "type": "number",
          "description": "AnchorUsd is the extreme a movement is measured from and AnchorAt when it\nwas observed; both absent for an absolute watch, and for a movement whose\nseries has no observation to anchor on."
        },
        "anchorAt": {
          "type": "string"
        },
        "state": {
          "type": "string",
          "enum": [
            "unpriced",
            "waiting",
            "waiting-on-history",
            "met"
          ],
          "description": "State is where the watch stands right now: \"met\" when the condition\nholds, \"waiting\" when it does not, \"waiting-on-history\" for a movement\nwhose record does not reach back as far as its own window — which can\nnever fire until it does — and \"unpriced\" when nothing prices the card."
        },
        "wouldFire": {
          "type": "boolean",
          "description": "WouldFire reports whether the next `hoard watch` would alert on this row.\n\nIt is not the same question as state == \"met\", and the difference is the\none thing about watches that surprises everybody: firing is edge\ntriggered, so a watch that crossed yesterday is still met today and will\nsay nothing. A supervisor loop reading exit codes alone cannot tell those\napart. This field can."
        },
        "lastFiredAt": {
          "type": "string",
          "description": "LastFiredAt is when the alert last went out (RFC 3339), absent on a watch\nthat has never fired. For a movement it is also the lower bound the next\nanchor is drawn from — firing re-anchors — so it dates the baseline as\nwell as the alert."
        },
        "createdAt": {
          "type": "string",
          "description": "CreatedAt is when the watch was set (RFC 3339). A movement cannot measure\na move that predates it, so this bounds the anchor too."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "id",
        "card",
        "op",
        "display",
        "state",
        "wouldFire"
      ],
      "description": "WatchRow is one standing watch: the rule, the price it is judged against, and where it stands."
    },
    "Watches": {
      "properties": {
        "rows": {
          "items": {
            "$ref": "#/$defs/WatchRow"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "rows"
      ],
      "description": "Watches is every standing watch and where each one stands — the machine reading of `hoard watch list`."
    }
  },
  "properties": {
    "schemaVersion": {
      "type": "string"
    },
    "kind": {
      "type": "string",
      "enum": [
        "summary",
        "holdings",
        "unpriced",
        "movers",
        "market",
        "report",
        "watch",
        "hoard",
        "watches",
        "binders",
        "guessed"
      ]
    },
    "summary": {
      "$ref": "#/$defs/Summary"
    },
    "holdings": {
      "$ref": "#/$defs/Holdings"
    },
    "unpriced": {
      "$ref": "#/$defs/Unpriced"
    },
    "movers": {
      "$ref": "#/$defs/Movers"
    },
    "market": {
      "$ref": "#/$defs/Market"
    },
    "report": {
      "$ref": "#/$defs/Report"
    },
    "watch": {
      "$ref": "#/$defs/WatchCheck"
    },
    "hoard": {
      "$ref": "#/$defs/Hoard"
    },
    "watches": {
      "$ref": "#/$defs/Watches"
    },
    "binders": {
      "$ref": "#/$defs/Binders"
    },
    "guessed": {
      "$ref": "#/$defs/Guessed"
    }
  },
  "additionalProperties": false,
  "type": "object",
  "required": [
    "schemaVersion",
    "kind"
  ],
  "description": "Document is the envelope every hoard JSON emission shares: a schema version, a kind, and the one payload the kind names."
}
